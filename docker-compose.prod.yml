version: '3.8'

services:
  gymsis-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gymsis-landing-${ENV:-prod}
    environment:
      - ENV=${ENV:-prod}
    ports:
      - "${PORT:-3002}:80"
    restart: unless-stopped
