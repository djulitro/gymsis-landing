version: '3.8'

services:
  gymsis-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gymsis-landing
    ports:
      - "${PORT:-3002}:80"
    restart: unless-stopped
